
// ФУНКЦИЈЕ У ЗАСЕБНОЈ ДАТОТЕЦИ

// именована `najveci` функција
function najveci(x, y) {
    return (x > y) ? x : y;
}

// неименована (анонимна) функција додијељена константи!
const najveci2 = function(x, y) {
    return (x > y) ? x : y;
}